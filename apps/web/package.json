{
  "name": "@paroliere/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "tsx scripts/sync-dictionary.ts",
    "dev": "vite",
    "prebuild": "tsx scripts/sync-dictionary.ts",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@paroliere/core": "workspace:*"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "svelte": "^5.15.0",
    "vite": "^6.0.7"
  }
}
