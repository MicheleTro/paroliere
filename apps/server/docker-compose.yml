services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: paroliere
      POSTGRES_PASSWORD: paroliere
      POSTGRES_DB: paroliere
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
